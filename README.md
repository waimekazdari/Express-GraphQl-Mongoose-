web app using express, mongoose, and graphQL

to run this App:

    run your command line and move to the application directory and then type:
          - npm install (to install all the dependencies from package.json file)
          - npm start

    
